var util = require('./util');
var fats = require('fats-kernel');

var initConfig = module.exports = function (opts) {

	var DEFAULT_INIT_CONFIG = {
		'src': './',
		'dist': './build'
	};
	var defaultCfg = DEFAULT_INIT_CONFIG;
	var root = process.cwd();
	util.extend(defaultCfg, opts);

	fats.taskConfig = defaultCfg

};
